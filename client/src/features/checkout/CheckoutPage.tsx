import { Typography } from '@mui/material';

export default function CheckoutPage() {
	return (
		<Typography variant="h3">
			Only authorized users can access this page. Please log in to continue.
		</Typography>
	)
}